<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="re.yuugu.hzx.infrastructure.persistent.dao.IGachaActivitySkuDao">

    <resultMap id="dataMap" type="re.yuugu.hzx.infrastructure.persistent.po.GachaActivitySku">
        <id column="id" property="id"/>
        <result column="sku" property="sku"/>
        <result column="activity_id" property="activityId"/>
        <result column="activity_count_id" property="activityCountId"/>
        <result column="stock_count" property="stockCount"/>
        <result column="stock_count_surplus" property="stockCountSurplus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

    </resultMap>

    <select id="queryGachaActivitySku" resultMap="dataMap">
        SELECT sku,activity_id,activity_count_id,stock_count,stock_count_surplus
        FROM gacha_activity_sku
        where sku=#{sku}
    </select>
    <update id="updateSkuStock">
        update gacha_activity_sku
        set stock_count_surplus = stock_count_surplus - 1,
            update_time = now()
        WHERE sku = #{sku}
          AND stock_count_surplus > 0
    </update>
    <update id="clearSkuStock">
        update gacha_activity_sku
        set stock_count_surplus = 0,
            update_time = now()
        WHERE sku = #{sku}
    </update>

</mapper>
